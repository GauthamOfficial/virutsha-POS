# Where the POS backend lives.
#
# LAPTOP SETUP (recommended): leave this EMPTY or delete the line.
#   The backend serves the built app itself, so everything is on one address.
#
# TWO-VERCEL-PROJECTS SETUP: set this to your backend's URL, e.g.
#   VITE_BACKEND_URL=https://visa-pos-api.vercel.app
VITE_BACKEND_URL=
